# EverCart Docker Environment Configuration
# This file contains environment variables for containerized deployment

# Node.js Environment
NODE_ENV=production

# Server Configuration
PORT=3000

# MongoDB Database Configuration
# This URI connects to the MongoDB container defined in docker-compose.yml
MONGODB_URI=mongodb://admin:evercart2025@mongodb:27017/evercart?authSource=admin

# Admin Configuration
# Secret key for creating admin accounts
ADMIN_KEY=EVERCART_ADMIN_2025

# Security Configuration
# Number of salt rounds for password hashing
BCRYPT_SALT_ROUNDS=12

# Optional: CORS Configuration (if needed for specific frontend domains)
# CORS_ORIGIN=http://localhost:3000

# Optional: Session Configuration (if you add sessions in the future)
# SESSION_SECRET=evercart_super_secret_session_key_2025

# Optional: JWT Configuration (if you implement JWT authentication)
# JWT_SECRET=evercart_jwt_secret_key_2025
# JWT_EXPIRES_IN=7d

# Optional: Email Configuration (for future email notifications)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# Optional: File Upload Configuration
# MAX_FILE_SIZE=5242880
# UPLOAD_PATH=/app/uploads

# Optional: Rate Limiting Configuration
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Optional: Logging Configuration
# LOG_LEVEL=info
# LOG_FILE=/app/logs/app.log
